# Architecture

This project is planned as a cross-platform podcast downloader with:

- a Rust core library for feed discovery, downloads, persistence, and scheduling logic
- a thin Rust CLI for local automation and debugging
- a Tauri desktop app for everyday use

Implementation details will be added after the repo structure and product plan are settled.

